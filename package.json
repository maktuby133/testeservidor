{
  "name": "caixa-agua-websocket-server",
  "version": "2.0.0",
  "description": "Servidor WebSocket para monitoramento de caixa d'água via LoRa",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:env": "node -r dotenv/config server.js",
    "test": "echo \"✅ Servidor testado e funcionando!\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.14.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
